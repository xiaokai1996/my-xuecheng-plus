#测试类不需要启动servlet，也不需要swagger，但是需要一个启动类
#微服务配置
spring:
  application:
    name: content-service
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost/xuecheng_content?serverTimezone=UTC&userUnicode=true&useSSL=false&
    username: root
    password: 12345678
# 日志文件配置路径
logging:
  config: classpath:log4j2-dev.xml
